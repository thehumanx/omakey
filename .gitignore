*.iml
.gradle/
build/
local.properties
.idea/
.DS_Store
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Internal design/technical-decisions doc — not for the public repo.
AGENTS.md

# Release signing key — never commit. Regenerating it would break update installs for anyone
# who already sideloaded a release build (Android requires matching signatures to upgrade).
keystore/
